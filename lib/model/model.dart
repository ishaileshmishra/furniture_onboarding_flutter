import 'package:flutter/material.dart';

class PageModel {
  final Color color;
  final String image;
  final String title;
  final String subtitle;

  const PageModel(this.color, this.image, this.title, this.subtitle);
}
